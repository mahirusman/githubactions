name: CI/CD counter app

on:
  push:
    branches: [master]

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v2

      - name: Go to dir
        run: /home/ubuntu/project/githubactions

      - name: make pull
        run: git pull

      - name: restart the server
        run: pm2 restart index.js
